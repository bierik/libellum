<h1>Flat Templates</h1>

<div class="list-group">
  <% @flat_templates.each do |flat_template| %>
    <div class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
      <div>
        <h5 class="mb-1">
          <%= link_to flat_template.name, flat_template %>
        </h5>
        <p class="mb-1">
          <%= number_to_currency(flat_template.price) %>
        </p>
      </div>
      <%= link_to flat_template, class: 'btn text-danger shadow-none', method: :delete, data: { confirm: 'Wosch würk?' } do %>
        <i class="fas fa-trash-alt"></i>
      <% end %>
    </div>
  <% end %>
</div>

<br>
<%= link_to 'New Flat Template', new_flat_template_path %>
