<div class="d-flex justify-content-between align-items-center mb-3">
  <h1>Kunden</h1>
  <%= link_to new_customer_path, class: 'btn btn-primary' do %>
    <%= fa_icon 'plus', text: 'Hinzufügen' %>
  <% end %>
</div>

<div class="list-group">
  <% @customers.each do |customer| %>
    <div class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
      <div>
        <h5 class="mb-1">
          <%= link_to customer.full_name, customer_path(customer) %>
        </h5>
        <p class="mb-1">
          <%= customer.address %>
        </p>
      </div>
      <%= link_to customer, class: 'btn text-danger shadow-none', method: :delete, data: { confirm: 'Wosch würk?' } do %>
        <%= fa_icon 'trash-alt' %>
      <% end %>
    </div>
  <% end %>
</div>
